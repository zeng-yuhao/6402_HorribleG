# 阶段 5.1：YYN 钥匙拾取修复（已安装；文件名保留作历史记录）

用户 PIE 日志中，三个 `BP_Key` 分别打印了 `Key A`，但没有 `YJX key acquired`。只读检查确认关卡里的任务实例 `Require BP Key=True`、`Consume Key On Submit=True`、`Required Key ID=YYN_Key`，三个钥匙的 `TargetDoor` 均有效，控制器钥匙组件追踪的类路径也是 `/Game/YYN/Blueprint_YYN/BP_Key.BP_Key_C`。因此不是这些实例参数漏设置；上一版组件单靠 Actor 销毁事件识别拾取，在这个关卡未能可靠接住事件。

`UE56_KeyFix/Plugins/HorrorTriggerPack` 已在 UE 5.6 下完成编辑器、游戏开发版和 Shipping 版构建。修复版对现有 `BP_Key` 的 `Sphere / 球形碰撞` 重叠直接记录钥匙，同时定时发现 World Partition 后加载的钥匙 Actor，并保留销毁监听作后备；`BP_Key` 和 `BP_Door` 蓝图不修改。

用户已关闭目标 UE 编辑器；目标旧插件备份在 `YJX/TargetBackups/before_key_overlap_fix`，修复版已安装到 `Plugins/HorrorTriggerPack`。UE 5.6 只读加载检查确认钥匙组件 1 个、追踪类路径正确、刷新间隔 0.5 秒，任务默认要求并消耗 `YYN_Key`，0 错误。游戏内 PIE 仍待用户复测，操作见 `使用说明_YYN钥匙任务.md`。
